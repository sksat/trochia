name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v1
      - name: Run a build
        run: |
          mkdir build
          cd build
          cmake ..
          make
